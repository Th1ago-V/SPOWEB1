let senha = "";
while (senha != "123") {
senha = prompt("Digite a senha:");
}
// R: Para que essa lógica funcione, deve se usar != ao invés de =
// Isso por que o while deve continuar enquanto a senha for diferente (!=) de 123
